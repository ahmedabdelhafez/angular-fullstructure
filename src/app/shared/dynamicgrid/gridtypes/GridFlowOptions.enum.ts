/**
 * @description an `enum` for `grid-auto-flow` in CSS3 grid that specify the options for new
 * added `Columns` or `Rows` and apply logic on it for the selected option
 */
export enum GridFlowOption {
  COLUMN = "column",
  ROW = "row"
  // DENSE = "dense"
}
