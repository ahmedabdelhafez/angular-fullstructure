export interface GridGap {
  /**
   * @description space between rows
   * @property `rowGap` value in pixel
   * @default 0px
   */
  rowGap?: number;
  /**
   * @description space between columns
   * @property `rowGap` value in pixel
   * @default 0px
   */
  columnGap?: number;
}
