/**
 * @description an initial option to determine the shape of the grid is it responsive by the `CSS3` grid system or have
 * a number of static columns defined by user
 */
export enum GridColumnOptions {
  FIXED = "fixed",
  DYNAMIC = "dynamic"
  // RESPONSIVE_SIZE_COLUMN = "rsc"
}
