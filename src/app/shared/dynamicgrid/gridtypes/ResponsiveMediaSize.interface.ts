/**
 * @description this interface used to apply size for all defined media query
 * all size value in `px` pixel
 */
export interface ResponsiveMediaSize {
  xlSize: number;
  lgSize: number;
  mdSize: number;
  smSize: number;
  xsSize: number;
}
