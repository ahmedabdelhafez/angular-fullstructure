import { GridcontainerDirective } from './gridcontainer.directive';

describe('GridcontainerDirective', () => {
  it('should create an instance', () => {
    const directive = new GridcontainerDirective();
    expect(directive).toBeTruthy();
  });
});
