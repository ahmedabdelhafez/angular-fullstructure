import { ItemvalidationDirective } from './itemvalidation.directive';

describe('ItemvalidationDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemvalidationDirective();
    expect(directive).toBeTruthy();
  });
});
