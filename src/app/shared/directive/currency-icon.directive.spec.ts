import { CurrencyIconDirective } from './currency-icon.directive';

describe('CurrencyIconDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrencyIconDirective();
    expect(directive).toBeTruthy();
  });
});
