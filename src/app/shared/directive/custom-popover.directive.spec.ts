import { CustomPopoverDirective } from './custom-popover.directive';

describe('CustomPopoverDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomPopoverDirective();
    expect(directive).toBeTruthy();
  });
});
